{
  "name": "devloop-native-fixture",
  "private": true,
  "version": "0.0.0",
  "description": "Minimal React Native + react-native-web project for the cockpit GUI target-switcher test. No `expo` dependency, so nativeInfo detects react-native + a Web target from react-native-web WITHOUT spawning `expo config` — the switcher renders in CI without installing anything.",
  "dependencies": {
    "react": "19.2.0",
    "react-native": "0.83.4",
    "react-native-web": "~0.21.0"
  }
}
